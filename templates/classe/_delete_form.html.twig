<form method="post" action="{{ path('app_classe_delete', {'id': classe.idClasse}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classe.idClasse) }}">
    <button class="btn btn-danger">Delete</button>
</form>
