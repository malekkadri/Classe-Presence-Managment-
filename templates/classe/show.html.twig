{% extends 'baseback.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}
    <h1>Classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.idClasse }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ classe.nom }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ classe.nombre }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ classe.niveau }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ classe.filiere }}</td>
            </tr>
        </tbody>
    </table>

   <button type="button" class="btn btn-secondary"><a href="{{ path('app_classe_index') }}">back to list</a></button>

  <button type="button" class="btn btn-warning"><a href="{{ path('app_classe_edit', {'id': classe.idClasse}) }}">edit</a></button>

    {{ include('classe/_delete_form.html.twig') }}
{% endblock %}
