{% extends 'baseback.html.twig' %}

{% block title %}Presence{% endblock %}

{% block body %}
    <h1>Presence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ presence.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ presence.date ? presence.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Seance</th>
                <td>{{ presence.seance }}</td>
            </tr>
        </tbody>
    </table>

    <button type="button" class="btn btn-secondary"><a href="{{ path('app_presence_index') }}">back to list</a></button>

    <button type="button" class="btn btn-warning"><a href="{{ path('app_presence_edit', {'id': presence.id}) }}">edit</a></button>

    {{ include('presence/_delete_form.html.twig') }}
{% endblock %}
